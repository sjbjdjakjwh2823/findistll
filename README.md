# Preciso (Public)

Minimal public-facing overview.

## What this is
FinDistill v11.5: a FastAPI-based financial data distillation engine.

## Quick start (high level)
- Install dependencies.
- Configure `.env` from `.env.example`.
- Run the service.

## Docs
See `README_DEPLOY.md` for a concise deployment checklist.
